Usage: nfzf [options]

Options:

-n, --normal        Use normal text matching instead of fuzzy matching.
                    Usually path to public directory.

Examples:

  find . | nfzf
  cat log.txt | nfzf -n
